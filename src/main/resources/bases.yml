# Global base schematics (files in plugins/RaidRiot/schematics/)
# Paste normalization: lowest solid block sits at world Y=0; spawn uses solid footprint center at spawn-y (config)
easy: "EasyBase.schematic"
medium: "MediumBase.schematic"
hard: "HardBase.schematic"

# schematic-center-from-min offsets (dx, dy, dz from paste origin) are in config.yml per difficulty
# Cached analysis per file is stored under schematics when a base is first prepared
schematics: {}
